# toggle-visibility-package

Simple npm package for toggling visibility of an element

For example, you could set up a button with a "click" event listener. Once the button is clicked, the event listener could trigger a call to toggleVisibility. You would then pass in the element you want to hide or make visible.

toggleVisibility(element)
